<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:dubbo="http://dubbo.apache.org/schema/dubbo"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://dubbo.apache.org/schema/dubbo http://dubbo.apache.org/schema/dubbo/dubbo.xsd">

	<!-- 消费方应用名，用于计算依赖关系，不是匹配条件，不要与提供方一样 -->
	<dubbo:application name="api-app">
		<dubbo:parameter key="qos.enable" value="false" />
	</dubbo:application>

	<!-- 使用ZK注册中心暴露发现服务地址 -->
	<dubbo:registry protocol="zookeeper"
		address="127.0.0.1:2181,127.0.0.1:2182,127.0.0.1:2183" /> <!-- client="zkclient" -->

	<dubbo:consumer timeout="5000" />

	<!-- 生成远程服务代理，可以和本地bean一样使用demoService -->
	<dubbo:reference id="storageService"
		interface="com.spike.codesnippet.seata.storage.service.StorageService" />
	<dubbo:reference id="orderService"
		interface="com.spike.codesnippet.seata.order.service.OrderService" />

	<bean id="apiService"
		class="com.spike.codesnippet.seata.api.service.impl.ApiServiceImpl">
		<property name="storageService" ref="storageService" />
		<property name="orderService" ref="orderService" />
	</bean>

	<!-- seata -->
	<bean class="io.seata.spring.annotation.GlobalTransactionScanner">
		<constructor-arg value="api-app" />
		<constructor-arg value="my_test_tx_group" />
	</bean>
</beans>