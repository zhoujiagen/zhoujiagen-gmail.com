<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper
	namespace="com.spike.codesnippet.seata.account.dao.AccountMapper">
	<resultMap id="BaseResultMap"
		type="com.spike.codesnippet.seata.account.domain.Account">
		<id column="id" property="id" jdbcType="INTEGER" />
		<result column="user_id" property="userId" jdbcType="VARCHAR" />
		<result column="money" property="money" jdbcType="INTEGER" />
	</resultMap>
	<delete id="deleteByPrimaryKey"
		parameterType="java.lang.Integer">
		delete from account_tbl
		where id = #{id,jdbcType=INTEGER}
	</delete>
	<insert id="insert"
		parameterType="com.spike.codesnippet.seata.account.domain.Account">
		insert into account_tbl (id, user_id, money
		)
		values
		(#{id,jdbcType=INTEGER}, #{userId,jdbcType=VARCHAR},
		#{money,jdbcType=INTEGER}
		)
	</insert>

	<update id="debit">
		UPDATE account_tbl
		SET money = money - #{money}
		WHERE user_id = #{userId}
	</update>

	<update id="updateByPrimaryKey"
		parameterType="com.spike.codesnippet.seata.account.domain.Account">
		update account_tbl
		set user_id =
		#{userId,jdbcType=VARCHAR},
		money = #{money,jdbcType=INTEGER}
		where id =
		#{id,jdbcType=INTEGER}
	</update>
	<select id="selectByPrimaryKey" resultMap="BaseResultMap"
		parameterType="java.lang.Integer">
		select id, user_id, money
		from account_tbl
		where id =
		#{id,jdbcType=INTEGER}
	</select>
	<select id="selectAll" resultMap="BaseResultMap">
		select id, user_id, money
		from
		account_tbl
	</select>
</mapper>